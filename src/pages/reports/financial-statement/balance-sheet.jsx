import { Card, Typography } from 'antd';

const { Title } = Typography;

const BalanceSheet = () => {
  return (
    <div>
      <Title level={2}>Balance Sheet</Title>
      <Card>
        <p>Balance Sheet analysis and reporting</p>
      </Card>
    </div>
  );
};

export default BalanceSheet;
